1. learning = representation+ evaluation + optimization
2. generalization- No matter how much training data we have, new unseen examples will always appear.
3. beyound data, leraners need to embody some assumption or knowledge
4. ML get more from less (means, Good assumptions allow learning from limited data.)
5. Overliftting- runnining the risk of hallucinating a classifier that is not grounded in reality
6. Overliftting has many faces-decomposing generalization error into bias and variance.
    Bias is a learner’s tendency to consistently learn the same wrong thing.
    Variance is the tendency to learn random things irrespective of the real signal.
7. Generalizing correctly becomes exponentially harder as the dimensionality (number of features) of the examples grows, because a fixed-size training set covers a dwindling fraction of the input space
8. more data can beats an algorithm
9. more simplicity, less accuracy
10. learn many models as much as possible

what is ML?
Ml is training a machine based on a large dataset and assumption so that the prediction can be more accurate.
gpt-Machine Learning is the study of algorithms that learn patterns from data using assumptions (inductive bias) to make accurate predictions on unseen data.
3 insights from the paper-(A Few Useful Things to Know about Machine Learning
Pedro Domingos
Department of Computer Science and Engineering
University of Washington)

1. output is basically more depedent upon generalization
2. overliftting decompose generalization error into bias and variance
3. learn many model so that implimentation can become more accurate and realistic
gpt-
🔹 Refined insights:

Generalization matters more than performance on training data.

Overfitting explains error through bias–variance tradeoff, not just model complexity.

Trying multiple models is necessary because no single model works best for all problems.


1 qu i have-
isn't it's getting more complex to implement a ML in real life as unrealistic tests, multiple models, dimentional challanges appears?